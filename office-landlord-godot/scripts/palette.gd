## Palette — Office Landlord's colours, one place.
##
## All-ages candy bucket per STANDARD.md: bright ground, saturated accents, cheerful.
## This is the deliberate opposite number of play/overtime-idle-godot/scripts/palette.gd
## (plum-black / hot magenta) -- the STANDARD's rule that a parent and its adult fork must
## differ in composition and palette, not just in a lights-down filter.
class_name Palette

const GROUND := Color("FFEFC8")        # warm cream, not white
const PANEL := Color("FFFFFF")
const PANEL_EDGE := Color("FFCC66")
const ACCENT := Color("FF6E4A")        # coral-orange: buttons, the pull-to-collect action
const GOLD := Color("FFC233")          # rent / currency
const HEAT := Color("2FB6A3")          # teal: momentum, "closer"
const SUCCESS := Color("3FCB6B")
const TEXT := Color("2B2118")          # warm near-black, never pure black
const MUTED := Color("9A7B4F")
const SKY := Color("8FD3FF")

const GROUND_DEEP := Color("FFE29A")
const PANEL_RAISED := Color("FFF6E0")
const ACCENT_DEEP := Color("E4502D")
const ACCENT_SOFT := Color("FF9C82")
const GOLD_DEEP := Color("E0A400")
const INK := Color("2B2118")
