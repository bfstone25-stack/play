# itch.io page kit — Overnight Clause

Clean slug `overnight-clause`, adult flag on, **no** shared page or crosslink with
`late-inspection` (the mainstream title is on Newgrounds/CrazyGames/Poki/GameDistribution;
Newgrounds already pulled an AI game of ours, see memory `newgrounds-no-ai-games`).

- Paid project, min price **$2.99**, suggested **$4.99** — mirrors
  `itch-analytics/profiles-final/late-inspection.json`.
- Free browser slice = the `Web Slice` export (`custom_features="slice"`): through the
  kitchen wall, the stain choice and the mirror plate, ending one beat before `cg_hatch`.
  Enforced by `SLICE_LAST_STAGE = 5`; test `tests/slice_gate.gd`.
- Tags: `adult,nsfw,horror,psychological-horror,first-person,visual-novel,atmospheric,multiple-endings,supernatural,3d`
- Genre `visual-novel`. Theme keeps the base palette (`itch-analytics/themes/late-inspection.json`
  — bg `#0d0f0a`, text `#e3e0d2`, link `#8aa65e`) so the two products read as one studio
  without sharing a page.
- Description: `itch-analytics/copy/overnight-clause-desc.html`.
- **Do not hand-edit pricing in the itch form** — memory `itch-edit-form-repost`: the edit
  form is a full re-POST and a wrong `min_price` re-priced two products on 2026-09-17. Use
  `itch-analytics/itch_set_tags.py`.
